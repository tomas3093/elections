import { TwoDigitPercentagePipe } from './two-digit-percentage.pipe';

describe('TwoDigitPercentagePipe', () => {
  it('create an instance', () => {
    const pipe = new TwoDigitPercentagePipe();
    expect(pipe).toBeTruthy();
  });
});
